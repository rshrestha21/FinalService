@model IEnumerable<Services.Models.Servicetype>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Servicekind)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Servicekind)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServicetypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServicetypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServicetypeId })
        </td>
    </tr>
}

</table>
